class App {
  constructor() {
    this.elements = {};
    console.log("App Initialized");
  }

  // navigation controls based on keyboard input and button controls
  // animate sprite on active
}

document.addEventListener("DOMContentLoaded", () => {
  window.app = new App();
});
